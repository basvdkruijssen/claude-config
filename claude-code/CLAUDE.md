<!--
Global Claude Code instructions, symlinked to ~/.claude/CLAUDE.md by
scripts/link-global-claude-md.(sh|ps1). Edit this file, commit, pull on your
other machines, done — no per-machine copy/paste.

This is intentionally close to empty. Fill it in yourself as you notice
durable preferences (tone, workflow habits, defaults you keep repeating to
Claude) — the same content you'd otherwise let drift into memory files that
only exist on one machine. Keep it short; this loads into every session.
-->

# Global preferences
