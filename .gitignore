# chezmoi local state (never belongs in the repo)
chezmoistate.boltdb
.chezmoi.toml
.chezmoi.yaml
.chezmoi.json

# Secrets and machine-local overrides
*.local
*.local.*
.env
.env.*
*.key
*.pem
*.pfx
secrets.toml

# Terraform (in case example code is ever added)
.terraform/
*.tfstate
*.tfstate.*
tfplan

# Operating system
.DS_Store
Thumbs.db
desktop.ini

# Editors
.idea/
*.swp
*~

# Machine-specific profile additions (never commit)
profile.local.ps1
init.local.sh

# Build output from scripts/package-desktop-skills.(sh|ps1)
dist/

# Claude Code runtime state left in this checkout by sessions run from it
.claude/scheduled_tasks.lock
